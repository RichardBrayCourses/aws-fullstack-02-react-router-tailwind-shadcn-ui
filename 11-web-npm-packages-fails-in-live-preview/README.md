To attempt to use this program in chrome run this command first in the folder containing this file:

```
npm install
```

Then open index.html using the microsoft live preview extension, and then in chrome.

In the console window you will see that chrome is unable to resolve package imports.
